import { Date1Directive } from './date1.directive';

describe('Date1Directive', () => {
  it('should create an instance', () => {
    const directive = new Date1Directive();
    expect(directive).toBeTruthy();
  });
});
